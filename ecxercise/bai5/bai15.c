#include <stdio.h>
#include <string.h>
#include <stdlib.h>

int tong(char *a, char *b){
    int so;
    int heso = 1;
    int len1, len2;
    if(strlen(a) > strlen(b)){
        len1 = strlen(a);
        len2 = strlen(b);
    }
    else{
        len1 = strlen(b);
        len2 = strlen(a);
    }
    for(int i = len2; i  > 0; i++){
        so = atoi(a[len]) 
    }
    return so;
}
int main(){

    return 0;
}